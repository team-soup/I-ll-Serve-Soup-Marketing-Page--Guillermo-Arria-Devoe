let topContainer = document.querySelector('.container');

topContainerChildren = Array.from(topContainer.children);

console.log(topContainerChildren)

// topContainerChildren.map(child => child.style.display = 'none');